<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<%- include('../partials/head.ejs') %>
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

    <body>
        <!-- navigation -->
        <%- include('../partials/navbar.ejs') %>

            <!-- left navbar -->
            <div class="container">
                <div class="row content">
                    <%- include('../partials/leftnav.ejs') %>

                        <div class="col-md-10">
                            <!-- Description -->
                            <h4>Query String</h4>
                            <hr />
                            <p>
                                This tool is a Query String Submission Tool designed to facilitate the creation and submission of query strings.
                            </p>
                            <ul>
                                <li>Input a raw query string directly.</li>
                                <li>Upon clicking the "Submit" button, the tool sends the constructed query string as a request to the server (/tool/querystring?key=value).</li>
                                <li>The server echoes back the submitted response status code and query string for verification.</li>
                            </ul>
                            <p>
                                <strong>Use Case:</strong> This tool is ideal for testing and debugging query strings in web applications or APIs.
                            </p>


                            <!-- Raw Query String -->
                            <div class="mb-3">
                                <h5 class="mt-5">Raw Query String</h5>
                                <input type="text" class="form-control w-100 mt-3" id="rawQueryInput" name="rawQuery"
                                    min="1" step="1" required placeholder="key1=value1&key2=value2">
                            </div>
                            <!-- Raw Query Submit Button -->
                            <div class="d-flex align-items-center">
                                <button type="button" class="btn btn-primary mt-2" id="submitRawQueryButton">
                                    Submit
                                </button>
                                <div class="d-flex justify-content-center w-75">
                                    <span id="timerDisplay" class="mt-2" style="display: none;"></span>
                                </div>
                            </div>
                            <!-- Display Query String -->
                            <% if (queryString) { %>
                                <div class="alert alert-info mt-3">
                                    <strong>Server Status:</strong> <%= status %>
                                    <br><strong>Query String:</strong> <%= queryString %>
                                </div>
                            <% } %>
                            

                        </div>
                </div>
            </div>

            <!-- scripts -->
            <%- include('../partials/scripts.ejs') %>
                <script src="../js/querystring.js"></script>
    </body>

</html>